/**
 * Type surface of the module `bun run build:embed` generates from the Vite
 * output. The implementation is not committed; `../bundle.ts` falls back to a
 * placeholder shell when it has not been built.
 *
 * Keys are URL paths such as `/index.html` and `/assets/main-Bx1.js`.
 */
export declare const WEB_ASSETS: Record<string, string>;
